<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="android.example.com.visualizerpreferences">

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".VisualizerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <!-- TODO (7) Chỉnh sửa ở đây và bên trong của SettingsActivity để xử lý VisualizerActivity
    làm parent của SettingsActivity và "up button" hiển thị trên màn hình:
    https://developer.android.com/training/implementing-navigation/ancestral.html
    Bạn hãy dùng attribute android:launchMode singleTop-->

</manifest>